using Employees.Data;
using Employees.Models;

namespace Employees
{
    internal static class Program
    {
        [STAThread]
        static void Main()
        {
            Application.Run(new Main());
        }
    }
}
